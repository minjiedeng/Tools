# numpy.dot(a, b, out=None): If a is an N-D array and b is an M-D array (where M>=2), 
# it is a sum product over the last axis of a and the second-to-last axis of b. Thus,
# The last dimension of a must be the same size as the second-to-last dimension of b.

# Example:
import numpy as np


# In a sovereign default model, where default_states(nA,nB)
default_states = Vd_compat > self.Vc
self.default_prob[:, :] = np.dot(self.PA, default_states)

# If default_states(nA,nB,nx) with anther dimension, the above raises error.




